{
    "version": "2.0.0",
    "tasks":[
        {
            "label": "CMake: configure",
            "type": "shell",
            "command": "cmake --preset hello-config-debug",
            "group": {
                "kind": "build", // Ctrl+Shift+B will trigger this task
                "isDefault": true
            }
        },
        {
            "label": "CMake: build",
            "type": "shell",
            "command": "cmake --build build/hello-config-debug -- -j" ,
            "group": {
                "kind": "build", // Ctrl+Shift+B will trigger this task
                "isDefault": true
            }
        },
    ]   
}